---
"@sentinelrn/core": patch
"@sentinelrn/native": patch
"@sentinelrn/react": patch
---

Fix repository links and add package metadata. READMEs now point to the correct
GitHub repository, and each package declares `repository`, `homepage`, and `bugs`
so npm and tooling link to the right place.
