# React Native / Metro do not fully support pnpm's default symlinked node_modules
# (their Babel/codegen toolchain resolves some transitive deps, e.g. tslib, by
# walking a flat tree). A hoisted layout is the documented setting for RN + pnpm
# monorepos and is required for the example app to bundle.
node-linker=hoisted
